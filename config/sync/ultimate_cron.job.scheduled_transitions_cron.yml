uuid: 205a479c-daef-4210-a653-33966fbbfd57
langcode: en
status: true
dependencies:
  module:
    - scheduled_transitions
title: 'Default cron handler'
id: scheduled_transitions_cron
weight: 0
module: scheduled_transitions
callback: scheduled_transitions_cron
scheduler:
  id: simple
  configuration:
    rules:
      - '* * * * *'
launcher:
  id: serial
  configuration:
    timeouts:
      lock_timeout: 3600
    launcher:
      thread: 0
logger:
  id: database
  configuration:
    method: '3'
    expire: 1209600
    retain: 1000
