services:
  nidirect_proni.commands:
    class: Drupal\nidirect_proni\Drush\Commands\NidirectProniCommands
    arguments:
      - '@entity_type.manager'
      - '@redirect.repository'
    tags:
      - { name: drush.command }
