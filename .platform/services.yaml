# The name given to the MariaDB service (lowercase alphanumeric only).
db:
  # The type of your service (mariadb), which uses the format
  # 'type:version'. Be sure to consult the MariaDB documentation
  # (https://docs.platform.sh/configuration/services/mysql.html#supported-versions)
  # when choosing a version. If you specify a version number which is not available,
  # the CLI will return an error.
  type: mariadb:10.11
  # The disk attribute is the size of the persistent disk (in MB) allocated to the service.
  disk: 20480
  configuration:
    schemas:
      - main
    endpoints:
      admin:
        default_schema: main
        privileges:
          main: admin

# Changing the service name provisions a new service for the Solr 9.9 index.
solr_9_9:
  type: solr:9.9
  disk: 1024
  configuration:
    modules:
      - extraction
      - analysis-extras
      - langid
      - ltr
    cores:
      nidirect_index:
        conf_dir: !archive "solr_configsets/nidirect/conf/"
    endpoints:
      solr:
        core: nidirect_index

redis:
  type: redis:8.8
